%h1 Editing booking_email

= form_for :booking_email, url: school_teacher_booking_emails_path(@school_teacher) do |f|
  .field
    = f.label :body
    = f.text_area :body, value: @body
  .actions
    = f.submit 'Send email'
